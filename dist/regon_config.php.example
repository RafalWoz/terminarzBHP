<?php
/**
 * TerminyBHP - konfiguracja proxy GUS (REGON BIR 1.1).
 *
 * SKOPIUJ ten plik jako `regon_config.php` (bez .example) w katalogu
 * `/public_html/terminybhp/` na serwerze LH.pl (obok `regon_proxy.php`).
 *
 * Plik NIE JEST commitowany do repo (wpis w .gitignore) - klucz zostaje
 * wyłącznie na Twoim hostingu. Deploy FTP nie nadpisuje go (wrzucasz go
 * ręcznie jednorazowo).
 */

return [
    // 'test' (domyślnie - publiczny klucz testowy) lub 'prod'.
    'env'     => 'prod',

    // Klucz z https://api.stat.gov.pl/Home/RegonApi (zakładka "Zamów dostęp").
    // Dla 'env' => 'test' można zostawić pusty - użyty zostanie klucz publiczny.
    'api_key' => 'WSTAW_TUTAJ_SWOJ_KLUCZ',
];
